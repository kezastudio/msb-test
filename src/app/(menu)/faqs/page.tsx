import Faqs from "@/components/FAQS/Faqs";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="">
      <Faqs />
    </div>
  );
};

export default page;
